# 项目协作约定

- 分析 PDF 户型时必须采用业主确认的方位：页面上方为西、右侧为北、下方为东、左侧为南，不得套用“上北下南”。
- 方位信息发生修正后，必须重新验证全部采光和房间朝向结论，不能沿用修正前的布局建议。
- 提出、对比或修改任何格局方案时，必须用横竖线绘制的 ASCII 平面图展现，不能只用文字或表格描述；每个房间标注名称、开间进深和面积，变动处用 ★ 标出。
- ASCII 平面图沿用上述方位约定：上方为西、右侧为北、下方为东、左侧为南，并在图外标注方位箭头。
- 多个方案并列时，每个方案各画一张图，图后再列代价和取舍。
- 凡是关于图形本身的说明——房间尺寸与面积、家具和设备的位置与名称、分区划分、门窗与门垛做法——都必须直接标注到图纸里，不能只写在方案文档或对话回复中；图纸要能脱离文档独立看懂。
- 标注必须可读：文字不得溢出所属房间的边界，不得与墙线、家具或其他文字叠成一团。空间不足时优先缩短文案、改竖排或重排位置，而不是把说明退回文档。
